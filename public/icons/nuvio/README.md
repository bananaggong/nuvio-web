# NUVIO Icon Assets

Source of truth: Figma file `047uslcrVYYJNpXFRV2tmV`, component set `icons` (`14:458`).

Use these SVG files instead of the legacy PNG header icons. Code should reference
the paths through `src/components/icons/nuvio-icons.ts` so repeated icons stay in
one place.
